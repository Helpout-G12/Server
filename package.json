{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "mkdir data",
    "prestart": "mongod --dbpath data --logpath /dev/null --config mongo.config &",
    "start": "pm2 start index.js",
    "stop": "pm2 stop index.js",
    "restart": "pm2 restart index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^5.1.0",
    "nodemon": "^2.0.22",
    "pm2": "^5.3.0"
  }
}
